import * as React from 'react';
import { ScrollView, View, Text, Image } from 'react-native';
import { INavigation } from '../../../interface';

/**
 * 提现管理
 */
export default class WithdrawManagement extends React.Component<INavigation> {
  public render() {
    return (
      <ScrollView>

      </ScrollView>
    );
  }
}
